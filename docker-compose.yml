services:
  backend:
    image: countdown-backend
    build:
      context: backend
    volumes:
      - ${DB_LOCATION-./db}:/home/nonroot/db
    ports:
      - "8080:8080"
    networks:
      - backend

networks:
  backend:
    name: countdown-backend
    driver: bridge
